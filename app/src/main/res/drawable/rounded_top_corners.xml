<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:top="0dp" android:bottom="-8dp"> <!-- Adjust the bottom value based on your desired corner radius -->
        <shape android:shape="rectangle">
            <solid android:color="@color/colorCardBackground" /> <!-- Replace with your desired background color -->
            <corners
                android:topLeftRadius="16dp"
                android:topRightRadius="16dp" />
        </shape>
    </item>
</layer-list>
